Exactly ✅

static → you decide the storage/persistence

auto → compiler decides the type for you

Quick mental hook:

static = “I want this to stick around”

auto = “compiler, figure my type”

One-line:

auto → type-deduced

const → value-locked

static → memory-lifetime-locked

const static → shared & locked
