Recursion = function calling itself

Stack stores “where to return” and local state

Base case = stop condition (prevents infinite recursion)

Recursive case = breaks problem into smaller subproblems

-----------------------------------------

Self-call → the function must call itself

Recursive case → breaks the problem into smaller subproblems

Base case → stopping condition to prevent infinite recursion
